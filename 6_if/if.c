// The if statement is used when we want the program to make a decision 

// Check if a number is positive 
#include <stdio.h>

int main() {
    int number = 10; 

    if (number > 0) {
        printf("The number is positive."); 
    }

    return 0; 
}